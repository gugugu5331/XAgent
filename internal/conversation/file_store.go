package conversation

// The legacy JSON FileStore production surface was retired by the T3.18
// atomic Store cutover. The file remains as the approved M5 deletion marker;
// legacy JSON decoding is private to migration.go.
