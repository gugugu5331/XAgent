package provider

// This source file is intentionally inert. The former exported SSEEvent was
// moved behind the decoder boundary during T2.69. The file remains present as
// an M5 conditional deletion candidate until the approved deletion gate runs.
